import 'package:flutter/material.dart';

class PrePlannedWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pre-planned Widget'),
      ),
      body: Center(
        child: Text('This is the pre-planned widget screen'),
      ),
    );
  }
}
